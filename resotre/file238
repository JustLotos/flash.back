<template>
    <v-footer app padless>
        <v-row class="flex primary">
            <v-col class="text-center">
                <v-btn v-for="service in services" :key="service.path" :to="service.path" class="mx-4" icon text color="white">
                    <v-icon size="24px">{{ service.icon }}</v-icon>
                </v-btn>
            </v-col>
        </v-row>
    </v-footer>
</template>

<script lang="ts">
import { Component, Vue } from 'vue-property-decorator';
import {mdiGithub} from "@mdi/js";
import {Link} from "../../../../../Utils/types";
@Component
export default class BaseFooter extends Vue {
    services: Link[];
    constructor() {
        super();
        this.services = [
            { label: 'github', icon: mdiGithub, path: 'https://github.com/JustLotos/flash.back' }
        ];
    }
}


</script>